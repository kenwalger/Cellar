{
  "name": "cellar-monorepo",
  "private": true,
  "version": "1.0.0",
  "license": "UNLICENSED",
  "type": "module",
  "workspaces": [
    "packages/*",
    "functions/*",
    "app",
    "studio",
    "web"
  ],
  "devDependencies": {
    "@sanity/blueprints": "^0.27.0"
  },
  "scripts": {
    "build:core": "npm run build --workspace @cellar/core",
    "test": "npm test --workspace @cellar/core",
    "seed:build": "node studio/scripts/build-ndjson.mts"
  }
}
